#include <bits/stdc++.h>
#define FIN ios::sync_with_stdio(0);cout.tie(0);cin.tie(0)
using namespace std;
using ll = long long;
 
void solve(){
    int n, m; cin >> n >> m;    
    int faroles = (n * m + 1) / 2;
    cout << faroles << "\n";
}
 
int main() {
    FIN;
    int t; cin >> t;
    while (t--)
        solve();
    return 0;
}